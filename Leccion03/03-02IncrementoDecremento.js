//Incremento: ++
//Decremento: --
let a = 3;
let b = 2;
let z = 0;
console.log(z);

//Pre-incremento (el operador ++ por delante de la variable)
z = ++a; //primero se incrementa (pasa de 'a' a 'a+1'), y luego se asigna.
console.log(z);

//Post-incremento (el operador ++ después de la variable)
z = a++; //
console.log(z);
